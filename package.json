{
  "name": "tambola-caller",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A friendly, offline-ready Tambola caller for physical tickets.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs"
  },
  "engines": { "node": ">=22" }
}
